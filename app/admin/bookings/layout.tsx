// app/admin/bookings/layout.tsx
export default function AdminBookingsLayout({ children }: { children: React.ReactNode }) {
  // Layout “vacío” para satisfacer Next types/validator sin cambiar tu UI.
  return <>{children}</>;
}